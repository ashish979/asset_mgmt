<% if defined?(@audits) || defined?(@property_audits) %>
  $('#assetShowPageInfo').html("<%= escape_javascript(render :partial => 'asset_history' )%>");
<% else %>
  $('#assetShowPageInfo').html("<%= escape_javascript(render :partial => 'show_detail' )%>");
<% end %>
endProgress();
